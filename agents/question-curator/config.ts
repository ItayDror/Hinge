export const CITIES = ['New York, NY', 'Los Angeles, CA', 'Chicago, IL', 'Austin, TX']

/** How many fresh questions per kind to target per run. */
export const TARGET = { location: 8, time: 6, general: 6 }

/** Roughly 70% light / 30% deep. */
export const LIGHT_RATIO = 0.7

export const DEDUPE_THRESHOLD = 0.6
